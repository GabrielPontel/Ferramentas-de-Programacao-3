import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Aula17-08: Marcador de Truco',
  webDir: 'www'
};

export default config;
