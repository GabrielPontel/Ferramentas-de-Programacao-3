import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DiretivaEstraturaPage } from './diretiva-estratura.page';

describe('DiretivaEstraturaPage', () => {
  let component: DiretivaEstraturaPage;
  let fixture: ComponentFixture<DiretivaEstraturaPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(DiretivaEstraturaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
